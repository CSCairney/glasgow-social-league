// import {RootState} from "@/app/store";
//
// export const selectAccountNameById = (state: RootState, id: number | null): string | undefined => {
//     if (state.account.id === id) {
//         return state.accountState.name;
//     }
//     return undefined;
// };
